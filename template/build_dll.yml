name: Build

on:
  push:
    paths-ignore:
      - '.github/*'
      - '*.md'
      - "*.yml"
  pull_request:
    paths-ignore:
      - '.github/*'
      - '*.md'
      - "*.yml"
  workflow_dispatch:
    # allow manual trigger

jobs:
  build:
    permissions:
      contents: read
      packages: read
    uses: ServerEmus/workflows/.github/workflows/build_dll.yml@main
    with:
      ProjName: RENAME_ME
    secrets:
      TOKEN: ${{ secrets.GITHUB_TOKEN }}